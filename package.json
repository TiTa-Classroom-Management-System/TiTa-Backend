{
  "name": "tita-backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "dev": "mysql -u root -p tita < ./db/tita_dump.sql & nodemon ./bin/www",
    "commit-db": "mysqldump.exe -u root -p tita > ./db/tita_dump.sql"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "dotenv": "^8.2.0",
    "express": "~4.16.1",
    "morgan": "~1.9.1",
    "mysql": "^2.18.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
